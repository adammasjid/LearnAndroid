<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.TestApp">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.TestApp.fragment.ProfileActivity" />
        <activity android:name="com.example.TestApp.fragment.FlexibleFragment" />
        <activity android:name="com.example.TestApp.intent.MoveForResultActivity" />
        <activity android:name="com.example.TestApp.intent.MoveWithObjectActivity" />
        <activity android:name="com.example.TestApp.BarVolume" />
        <activity android:name="com.example.TestApp.intent.MoveWithDataActivity" />
        <activity android:name="com.example.TestApp.intent.MoveActivity" />
        <activity android:name="com.example.TestApp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Baris di atas bermakna bahwa MainActivity menjadi entry point ke aplikasi. -->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Kode di atas menandakan bahwa MainActivity akan dikategorikan sebagai activity launcher. -->
            </intent-filter>
        </activity>
    </application>

</manifest>